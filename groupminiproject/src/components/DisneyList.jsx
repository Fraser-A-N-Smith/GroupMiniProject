const DisneyList = (data) => {
    
    // const filterQuery = (query) => (list) => list.title.includes(query);
 
    // let filteredList = [];
    
    // if (completed) {
    //   filteredList = list.filter(filterQuery(query))
    // }
    // else if(notCompleted) {
    //   filteredList = list.filter(filterQuery(query))
    // }
    // else{
    //   filteredList = list.filter(filterQuery(query))
    // }
    
    // console.log(data);
    
      
    // printData();
    return (<div>
        <ul></ul>
    </div> );
}
 
export default DisneyList;